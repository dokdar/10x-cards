---
import AuthLayout from "@/layouts/AuthLayout.astro";
import ForgotPasswordForm from "@/components/auth/ForgotPasswordForm";

// Redirect authenticated users to home page
const { user } = Astro.locals;
if (user) {
  return Astro.redirect('/');
}
---

<AuthLayout title="Odzyskaj hasło - 10x Cards" description="Odzyskaj dostęp do konta 10x Cards">
  <main className="min-h-screen bg-background flex items-center justify-center px-4 py-8">
    <div className="w-full max-w-md mx-auto">
      <div className="space-y-8">
        {/* Header */}
        <div className="space-y-2 text-center">
          <h1 className="text-3xl font-bold tracking-tight">Odzyskaj hasło</h1>
          <p className="text-muted-foreground">Pomoż nam odnaleźć Twoje konto</p>
        </div>

        {/* Form Card */}
        <div className="bg-card rounded-lg border p-8 shadow-sm">
          <ForgotPasswordForm client:load />
        </div>
      </div>
    </div>
  </main>
</AuthLayout>
