---
import AuthLayout from "@/layouts/AuthLayout.astro";
import RegisterForm from "@/components/auth/RegisterForm";

// Redirect authenticated users to home page
const { user } = Astro.locals;
if (user) {
  return Astro.redirect('/');
}
---

<AuthLayout title="Rejestracja - 10x Cards" description="Zarejestruj się w aplikacji 10x Cards">
  <main className="flex-1 flex items-center justify-center px-4 py-8">
    <div className="w-full max-w-md">
      <div className="space-y-8">
        {/* Header */}
        <div className="space-y-2 text-center">
          <h1 className="text-3xl font-bold tracking-tight">Utwórz konto</h1>
          <p className="text-muted-foreground">Zacznij naukę z pełnym dostępem do aplikacji</p>
        </div>

        {/* Form Card */}
        <div className="bg-card rounded-lg border p-8 shadow-sm">
          <RegisterForm client:load />
        </div>
      </div>
    </div>
  </main>
</AuthLayout>
